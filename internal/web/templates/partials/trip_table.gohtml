{{define "trip-table"}}
<table>
  <thead>
    <tr><th>Date</th><th class="num">Start</th><th>Direction</th><th>Status</th><th class="num">Waypoints</th><th class="num">Duration</th><th class="num">Distance</th><th></th></tr>
  </thead>
  <tbody>
  {{range .}}
    <tr>
      <td>{{.LocalDate}}</td>
      <td class="num">{{hourLabel .LocalHour}}</td>
      <td>{{if .Direction}}{{.Direction}}{{else}}—{{end}}</td>
      <td><span class="badge {{.Status}}">{{.Status}}</span></td>
      <td class="num">{{.MatchedWaypoints}}</td>
      <td class="num">{{duration .DurationS}}</td>
      <td class="num">{{km .DistanceM}} km</td>
      <td><a href="/trips/{{.ID}}">open</a></td>
    </tr>
  {{end}}
  </tbody>
</table>
{{end}}
